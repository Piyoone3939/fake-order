# 現状エラー修正

- [x] `game_spec.md` と `CLAUDE.md` を確認する
- [x] Unity ログと C# ビルド結果から問題を特定する
- [x] `Terminal.cs` の CS0165 を修正する
- [x] Unity 6 の非推奨 API 警告と未使用フィールド警告を解消する
- [x] C# ビルドとログ再確認で検証する

## Review

- `Terminal.CompleteHacking` で、`CommandLog` が見つからない経路にも失敗理由を設定し、CS0165 を解消した。
- Unity 6000.4 で非推奨のオブジェクト検索 API を現行 API に置き換え、未使用の移動パラメーターを削除した。
- `dotnet build FakeOrder.slnx --no-restore` は警告 0、エラー 0 で成功した。
- `Editor.log` のプロジェクトコード由来エラーは修正対象の CS0165 のみ。旧 AssetImportWorker ログの assertion は Unity 内部のアセンブリ再読み込み時に発生しているが、ワーカーは復帰しており、現行ログでは再発していない。
- Unity Editor が同じプロジェクトを起動中のため、別プロセスによるバッチ起動は実施していない。

## 操作ガイド作成

- [x] 実装済みのキー入力とゲームフローを確認する
- [x] 起動方法、スパイ操作、オーガナイザー操作を文書化する
- [x] 現在のローカルプロトタイプに関する注意点を記載する

### Review

- ルート直下の `操作方法.md` に、現在のコードと一致する操作方法を追加した。

## シーンオブジェクトの編集モード配置

- [x] `GamePrototypeSetup` を編集モードで実行可能にする
- [x] 初回のみオブジェクトを生成し、Play時の再生成を止める
- [x] 編集モードとPlayモードの両方で安全な削除処理にする
- [x] 操作ガイドを新しい起動・ロール表示手順へ更新する
- [x] C#ビルドを警告0・エラー0で通す

### Review

- 旧シーンを編集モードで開くと、`GamePrototypeSetup` が一度だけ施設、両ロール、カメラ、UI用Canvasを配置するよう変更した。
- 生成完了フラグをシーンに保存し、配置後にユーザーが行った位置・設定変更をPlay開始時に上書きしないようにした。

## プロトタイプ細部修正

- [x] 疑惑値0で毎秒出続けるログを停止する
- [x] 移動していないShift入力を走行扱いしない
- [x] 疑惑100%の警告が連続発火しないようにする
- [x] 同一端末の再収集で収集件数が増えないようにする
- [x] 罠情報の正体がスパイUIから即座に分からないようにする
- [x] 新鮮な本物の情報だけを脱出判定に使用する
- [x] メインフェーズ開始時から15分を計測する
- [x] Build Settingsのシーン参照を`GamePrototype`へ修正する
- [x] C#ビルドを警告0・エラー0で通す

### Review

- 仕様上の「異なる3箇所」「罠は本物に見える」「メインフェーズ15分」と実装を一致させた。
- Unityログを埋めていた疑惑自然減衰の無効なログ出力を抑止した。

## Phase 1.5: ロール選択

- [x] Play開始時のロール選択画面を追加する
- [x] ボタンと数字キーの両方で選択できるようにする
- [x] 選択したロールのUIだけを表示する
- [x] 選択したロールの入力だけを有効化する
- [x] 準備フェーズの残り時間を表示する
- [x] ゲーム終了表示と`F1`による再選択を追加する
- [x] 再試合時に情報表示と疑惑値をリセットする
- [x] 既存シーンへ`RoleSelectionUI`と`EventSystem`を補完する
- [x] 操作ガイドを新しいフローへ更新する
- [x] C#ビルドを警告0・エラー0で通す

### Review

- 手動でCanvasを無効化する運用を廃止し、ゲーム内からロールを選択できるようにした。
- ロール選択、準備、メイン、終了、再選択のフェーズを一つの進行管理に統合した。

## Phase 1.5: 罠情報配置

- [x] `T`で開閉できる罠配置メニューを追加する
- [x] 数字キーで端末1～3を選択できるようにする
- [x] 仕様どおり10秒の配置進捗を実装する
- [x] `T`または`Esc`でキャンセルできるようにする
- [x] 配置済み端末をメニューに表示する
- [x] 準備フェーズ中からオーガナイザー操作を許可する
- [x] 再試合時に以前の罠情報をリセットする
- [x] スパイの端末収集処理へ罠状態を接続する
- [x] 操作ガイドを更新する
- [x] C#ビルドを警告0・エラー0で通す

### Review

- オーガナイザーによる端末選択、10秒設置、完了通知、スパイ側での偽装表示まで一連の流れを実装した。

## ロール選択・オーガナイザー画面修正

- [x] ロールボタンのクリックをInput Module非依存でも検出する
- [x] 二重選択を防止する
- [x] `Enter`で準備フェーズを省略できるようにする
- [x] オーガナイザーのマップ表示を正方形比率に固定する
- [x] マップ、カメラ、操作説明を分離したレイアウトへ変更する
- [x] 操作ガイドを更新する

### Review

- Unityログに実行時例外はなく、入力経路とレイアウト設計に原因があると判断して修正した。

## ドキュメント整理

- [x] `design_guide.md`のデザイン方針を確認する
- [x] `docs/`フォルダを作成する
- [x] 仕様・デザイン・画面遷移・操作資料を`docs/`へ移動する
- [x] `docs/README.md`にドキュメント索引を作成する
- [x] Markdownリンク切れがないことを確認する

### Review

- `CLAUDE.md`は開発ルール、`tasks/`は作業記録として元の位置に残した。
- 実画面デザインは`docs/design_guide.md`の「明るい官僚組織 × 情報の違和感」を基準にする。

## Figma UIデザイン v1

- [x] デザインガイドと画面遷移図をFigmaの構成へ反映する
- [x] カラー、タイポグラフィ、余白、影の基礎定義を作成する
- [x] 共通コンポーネントを作成する
- [x] タイトルからリザルトまでの7画面を作成する
- [x] 端末操作、ハッキング、命令ログ、罠配置の補助UIを作成する
- [x] プレースホルダー、レイヤー名、フォント、主要画面を監査する
- [x] Figmaリンクと作成内容をドキュメント化する
- [x] ユーザー確認後にGitHubへプッシュする

### Review

- Figmaファイル「Fake Order — UI Design v1」を作成した。
- `docs/design_guide.md`の「明るい官僚組織 × 情報の違和感」を画面とコンポーネントへ反映した。
- 本案は画面構成を確認するための簡易デザインとして採用し、最終ビジュアルは別工程で再設計する。

## 正式ロゴ反映

- [x] 採用ロゴ画像とロゴ仕様書を確認する
- [x] ロゴ画像をUnityプロジェクト内へ保存する
- [x] ロゴ仕様書を`docs/`へ保存する
- [x] デザインガイドを正式ロゴの配色・余白規則へ更新する
- [x] Figmaの表紙とタイトル画面へ正式ロゴを反映する
- [x] Figma上で表示崩れがないことを画像確認する
- [x] ユーザー確認後にGitHubへプッシュする

### Review

- 横長フルロゴを正式案として採用し、Figmaの旧ワードマークは非表示で保持した。
- ロゴ画像は`FakeOrder/Assets/UI/Branding/fake_order_logo.png`へ保存した。
- GitHubへのプッシュはユーザー確認待ち。

## Phase 1.6: 3Dタイトルロゴ

- [x] 既存のタイトル遷移とカメラ構成を確認する
- [x] PNG貼り付けではない編集可能な3Dロゴを実装する
- [x] 金属フレーム、円形エンブレム、立体文字、命令書マークを実ジオメトリで構成する
- [x] 専用カメラ、照明、控えめなタイトルモーションを追加する
- [x] タイトルからロール選択へEnterまたはクリックで遷移させる
- [x] 既存シーンへ編集モード・Playモードの両方で自動補完する
- [x] 操作方法と設計資料を更新する
- [x] C#ビルドとUnityログで検証する

### Review

- 元PNGは形状の参考に限定し、タイトル表示には使用しない構成へ変更した。
- `TitleLogo3D/LogoAssembly`以下へプリミティブ形状を焼き込み、Hierarchyから編集できるようにした。
- 専用カメラ、キーライト、ブルーリムライトと、Play中の控えめな浮遊モーションを追加した。
- `Title`フェーズを追加し、Enterまたはクリック後にロール選択へ進むフローへ接続した。
- Unity実カメラで3D形状、陰影、画面内の収まりを確認した。
- `dotnet build FakeOrder/FakeOrder.slnx --no-restore`は警告0、エラー0で成功した。

## Phase 2.2b: 管理ルーム自由移動と端末操作

- [x] オーガナイザーを管理ルーム内のFPS操作へ変更する
- [x] 開始直後は認証画面を表示せず、部屋の中から開始する
- [x] 机の端末を注視して`E`で認証・監視画面を開く
- [x] 端末から離れる、または`E` / `Esc`で監視画面を閉じる
- [x] 管理ルームへ後続ゲーム要素用の設備を追加する
- [x] 操作方法と画面遷移を更新する
- [x] Unityシーン、実行ログ、C#ビルドを検証する

### Review

- オーガナイザーへCharacterControllerと専用FPSカメラを追加し、管理ルーム内をWASD・マウスで移動できるようにした。
- 開始時は3D管理ルームと最小HUDだけを表示し、机のモニターを3.2m以内から注視して`E`を押した場合だけ認証・監視UIを開く。
- 端末表示中も移動を許可し、端末から4.5m以上離れるか`E` / `Esc`を押すと、監視UI・命令ログ・罠配置をまとめて閉じる。
- 作戦卓、通信コンソール、証拠保管庫、緊急制御盤を追加し、後続フェーズで別作業を実装できる編集可能な土台にした。
- Unityバッチ検証でFPSカメラの直接描画、端末Collider、追加設備、3階12部署・6監視カメラの維持を確認した。
- `dotnet build FakeOrder/FakeOrder.slnx --no-restore`は警告0、エラー0で成功した。

## Phase 2.3: 添付間取り準拠の複雑フロア

- [x] 1Fを一般フロアのロビー・オープンオフィス・会議室・警備室などへ再構成する
- [x] 2Fを重要データフロアの監視室・データルーム・記録保管室などへ再構成する
- [x] 3Fを最上階の司令室・監視中枢・メインサーバー室などへ再構成する
- [x] 中央エレベーターと各階ロビーの動線を接続する
- [x] 2F/3Fパス保管室、マスターキー保管室、サーバー室へ認証扉を設定する
- [x] 情報端末、脱出地点、監視カメラを新間取りへ再配置する
- [x] 仕様資料と操作資料を更新する
- [x] Unityシーン、カメラ描画、C#ビルドを検証する

### Review

- 添付された1F～3Fの間取りを基に、中央エレベーターとロビーを核とする階別レイアウトへ置き換えた。
- 3階合計28室に加えて各階のロビー／オープン区画と中央エレベーターを設け、認証扉4枚、エレベーターパネル4基、情報端末3台、監視カメラ6台を配置した。
- 俯瞰マップは1F専用レイヤーだけを描画し、2F・3Fは室内角・通路端の監視映像だけで確認する情報制限を維持した。
- Unityバッチ検証で必須区画、認証扉、エレベーター、端末、監視カメラの生成とシーン保存を確認した。
- `dotnet build FakeOrder/FakeOrder.slnx --no-restore`は警告0、エラー0で成功した。

## Phase 2.4: 大型オフィス化とブラフPC

- [x] 3階施設の平面寸法を拡張し、移動距離と探索範囲を増やす
- [x] オープンオフィスへデスク島と多数の操作不能PCを配置する
- [x] 個室・分析室・監視室にも用途に応じたブラフPCを配置する
- [x] 椅子、収納、植栽などを追加してオフィス密度を上げる
- [x] 操作可能端末3台をブラフPC群の中で見分けにくい配置へ調整する
- [x] 監視カメラ、俯瞰マップ、認証扉、エレベーターを拡張寸法へ追従させる
- [x] シーン再生成、配置数、カメラ描画、C#ビルドを検証する

### Review

- 施設平面を44×30相当から61.6×42へ拡張し、部屋・ドア・端末・カメラ・エレベーターを同じ1.4倍の倍率で追従させた。
- 1Fオープンオフィスを中心に、個室、分析室、監視室、司令室などへ合計88台のブラフPCを配置した。
- 各ワークステーションをデスク、モニター、スタンド、椅子で構成し、植栽を混ぜてデスク島の単調さを抑えた。
- 操作可能端末3台も同じデスク型へ変更し、青い画面と`[E]`プロンプトでのみ識別できるようにした。
- Unityバッチ検証で大型床、ブラフPC88台、認証扉4枚、エレベーター4基、端末3台、監視カメラ6台を確認した。
- 共有マテリアル化後のUnityログに例外・マテリアルリーク警告はなく、C#ビルドは警告0、エラー0で成功した。

## Phase 2.4b: GameビューのNo cameras表示を除去

- [x] タイトル／ロール選択中にDisplay 1へ描画する背景カメラを追加する
- [x] Spy・Organizerカメラの有効化後に表示へ干渉しない描画順へ設定する
- [x] UnityシーンとC#ビルドを検証する

### Review

- タイトル／ロール選択中は両プレイヤーカメラが無効で、RenderTexture用カメラしか残らないことが表示原因だった。
- `DisplayBackdropCamera`をDisplay 1へ常時直接描画し、Culling Mask 0、Depth -100の最背面に設定した。
- SpyまたはOrganizerのカメラが有効になると、その映像が後から描画されるためゲーム画面へ干渉しない。
- Unityバッチ検証で背景カメラの有効状態、出力先、描画順を確認し、C#ビルドは警告0、エラー0で成功した。

## Phase 2.5: 1F NPC・警備システム縦切り

- [x] 1Fの家具・壁を考慮したNavMeshを生成する
- [x] 一般社員NPCを6人配置し、執務・休憩・ロビーの巡回ルートを設定する
- [x] 警備員NPCを2人配置し、ロビー・通路・重要区画を巡回させる
- [x] NPCの視野角、距離、壁による遮蔽を使ったスパイ検知を実装する
- [x] 走行・端末ハッキング中の発見を疑惑ゲージへ接続する
- [x] 疑惑100%でスパイ排除となるゲームループを接続する
- [x] NPCを1F俯瞰マップと監視カメラへ表示する
- [x] NPC数、NavMesh、シーン、C#ビルドを検証する
- [x] 操作資料と仕様資料を更新する

### Review

- Unity AI Navigationの`NavMeshSurface`を1Fへ追加し、床、壁、88台のブラフPC、椅子などのColliderを考慮したNavMeshを生成した。
- 一般社員6人と警備員2人をシーンへ焼き込み、全巡回区間について`NavMeshPathStatus.PathComplete`を検証した。
- 社員は距離9m・視野角70度、警備員は距離13m・視野角90度とし、Physics.Raycastで壁越し検知を防止した。
- 発見時の疑惑上昇へ走行1.8倍、端末操作2.2倍の補正を接続し、100%で`SpyEliminated`へ遷移するようにした。
- 1F専用レイヤーをNPCにも設定し、俯瞰マップと室内監視カメラの両方に社員・警備員が表示されることを画像確認した。
- NavMeshは`Assets/Navigation/FirstFloorNavMesh.asset`へ分離し、シーンをGit差分可能なYAML形式で維持した。
- Unityバッチ検証は社員6人、警備員2人、NavMesh 1231頂点、全経路到達可能で成功した。
- `dotnet build FakeOrder/FakeOrder.slnx --no-restore`は警告0、エラー0で成功した。

## Phase 2.6: 全階NPC展開と偽造命令連動

- [x] 2F・3Fへ階別NavMeshを追加する
- [x] 2Fへ社員6人・警備員2人を配置する
- [x] 3Fへ職員4人・警備員3人を配置する
- [x] 偽造した警備移動命令で同じ階の警備員を端末周辺へ誘導する
- [x] 点検命令で同じ階の一般NPCを端末周辺へ誘導する
- [x] 緊急命令で同じ階の全NPCを端末周辺へ誘導する
- [x] オーガナイザーが命令を無効化したらNPCを通常巡回へ戻す
- [x] 全階のNPC数、NavMesh、巡回経路、C#ビルドを検証する
- [x] 操作資料と仕様資料を更新する

### Review

- 1Fに社員6人・警備員2人、2Fに社員6人・警備員2人、3Fに職員4人・警備員3人を配置し、合計23人へ拡張した。
- 階別のNavMeshを3アセットへ分離し、合計3367頂点と全NPCの巡回区間が到達可能であることをUnityバッチ検証した。
- 警備移動・点検・緊急命令を同じ階の対象NPCへ接続し、35秒経過または命令無効化で通常巡回へ復帰するようにした。
- 2Fロビーと3F通路・オフィスの監視カメラ画像でNPC表示を確認した。
- `dotnet build FakeOrder/FakeOrder.slnx --no-restore`は警告0、エラー0で成功した。

## Phase 2.7: 遅延映像からの容疑者照合・排除

- [x] 各監視カメラの遅延フレームへ撮影時点のスパイ所在情報を記録する
- [x] 選択中のカメラ映像に対する長押し照合操作を実装する
- [x] 正しい照合でスパイ排除へ遷移する
- [x] 誤認時に認証ポイントを消費し、再照合まで待機時間を設ける
- [x] 監視UIへ照合進捗、残り認証ポイント、操作説明を表示する
- [x] カメラ範囲の階層分離検証とC#ビルドを確認する
- [ ] Playモードで照合成功・誤認の両経路を確認する
- [x] 操作資料と仕様資料を更新する

### Review

- 映像リングバッファの各スロットへ撮影時点のスパイ所在判定を保存し、映像と同じ遅延インデックスから照合するようにした。
- 選択中カメラで`R`を3秒間長押しすると照合し、成功時はスパイ排除、誤認時は認証ポイントを1点消費して10秒間再操作を停止する。
- 監視端末UIへ照合進捗、残り3点の認証ポイント、待機時間を表示するパネルを追加した。
- 6カメラの監視範囲について上下階を誤認しない自動検証を追加し、C#ビルドは警告0、エラー0で成功した。
- 起動中のメインUnity Editorと検証用EditorのライセンスIPCが競合したため、今回のPlayモード経路確認のみ未完了。

## Phase 1.6b: 透過PNGタイトルロゴへの切り替え

- [x] 3Dロゴ再構成と専用カメラを撤去する
- [x] 外側背景だけを透明化し、内部の白パネル・白線を保持する
- [x] `Assets/UI/Logo/FakeOrderLogo.png`へ透過PNGを保存する
- [x] Sprite、Single、Alpha、非圧縮、Bilinear、Max Size 2048を設定する
- [x] Screen Space Overlayと1920x1080・Match 0.5を設定する
- [x] `LogoRoot/LogoImage`へPreserve Aspect・Raycast無効で配置する
- [x] ロゴ画像を描き直さない非破壊エフェクトを追加する
- [x] タイトルからロール選択への遷移を維持する
- [x] C#ビルド、Unityログ、表示構造を検証する

### Review

- 元画像のRGB値を保ったまま外周のみをアルファ化し、四隅のAlpha=0と内部白パネルのAlpha=255を確認した。
- 3Dロゴ由来の74オブジェクトと292個のシーンYAMLブロックを撤去した。
- `RoleSelectionUI` が `LogoRoot/LogoImage` を作成し、画像自体を改変せずに別レイヤーの演出だけを加える構成にした。
- `Assembly-CSharp` と `Assembly-CSharp-Editor` はどちらも警告0、エラー0でビルド成功した。
- Unityバッチ実行でシーンを正常ロードし、透過Spriteの再インポート、参照割り当て、シーン保存を終了コード0で確認した。

## Phase 1.6c: タイトルUIの視認性と画質改善

- [x] ロゴのインポート時縮小を防ぐ
- [x] Canvasのピクセル境界を優先する
- [x] タイトル、ロール選択、ステータスの文字を大きくする
- [x] C#ビルドとUnityインポートを検証する

### Review

- 横2172pxの原画に対しMax Sizeを2048から4096へ上げ、インポート時の縮小を防いだ。
- RoleSelectionUIの文字を24〜36%、SpyUI・OrganizerUI・CommandLogを約18%拡大し、小さなラベルは個別に引き上げた。
- Screen Space OverlayのCanvasでPixel Perfectを有効化した。
- Unityバッチ実行はロゴ再インポートとシーン保存を終了コード0で完了した。

## Phase 2.1: 3階建て部署フロア改装

- [x] 1〜3階の部署構成と主動線を設計する
- [x] 各階に複数の部署・通路・盲点を配置する
- [x] 認証が必要な部屋とハッキング式ドアを実装する
- [x] 階移動用のエレベーターパネルを実装する
- [x] 監視カメラを室内・通路の角上部からの透視映像へ変更する
- [x] 俳瞰マップで1〜3階を同時に見通せないようにする
- [x] 既存の情報端末、罠、脱出判定を新フロアへ接続する
- [x] Unityシーン、C#ビルド、操作資料を検証する

### Review

- 1Fは受付・総務・人事・警備資料室、2Fは開発・運用・会議・財務、3Fは役員・情報分析・法務・サーバーの12部署で構成した。
- セキュリティ資料室、財務記録室、役員室、サーバー室に5秒ハッキング式認証ドアを配置した。
- 室内角3台、通路端3台の計6台を透視カメラとし、映像ごとに4〜35秒の遅延を設定した。
- 俳瞰マップは1Fだけを映す高さに制限し、2F・3Fは監視映像から推測する構成にした。
- GPU描画で通路・財務・サーバーカメラを確認し、壁が天井まで視界を遮ることを確認した。

## Phase 2.2: 管理ルームと監視画面連動

- [x] オーガナイザー開始時の管理ルームを3Dシーンとして配置する
- [x] 監視システムの認証・ロック解除フローを追加する
- [x] 右側の監視カメラをクリック選択できるようにする
- [x] 選択した遅延映像を左側の大画面へ表示する
- [x] `C`キーによるカメラ切替を大画面表示へ連動させる
- [x] 操作方法とゲーム仕様を更新する
- [x] Unity描画、C#ビルド、シーン保存を検証する

### Review

- オーガナイザー選択直後は白・ネイビー・青基調の3D管理ルームと認証端末を表示し、1.5秒の認証完了後だけ監視・命令ログ・罠配置を解放する構成にした。
- 右側の6カメラセル全体をクリック可能にし、クリックと`C`の両方を同じ選択処理へ接続した。選択中の遅延フレーム、エリア名、遅延時間、映像時刻を左の16:9大画面へ表示する。
- シーンには管理ルーム、専用カメラ、机、認証端末、壁面6モニターを保存し、既存の3階12部署・6監視カメラも維持した。
- Unity Editor実行ログで6台のカメラ選択が順番に動作し、実行時例外が無いことを確認した。管理ルーム専用カメラの描画画像も確認した。
- `dotnet build FakeOrder/FakeOrder.slnx --no-restore`は警告0、エラー0で成功した。
